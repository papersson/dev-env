return {
  {
    "echasnovski/mini.surround",
    opts = {
      -- These are the default mappings, you can change them:
      mappings = {
        add = "sa", -- Add surroundings with 'sa' instead of 'gz'
        delete = "sd", -- Delete surroundings
        replace = "sr", -- Replace surroundings
      },
    },
  },
}
